import { View, Text, TextInput, ScrollView, SafeAreaView } from 'react-native'
import supabase from '../supabase'

const Auth = () =>  {
  return (
    <SafeAreaView>
      <View>
        <Text>Welcome</Text>
      </View>
    </SafeAreaView>
  )
}

export default Auth
